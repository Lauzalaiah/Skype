{
  "name": "skip",
  "version": "8.139.0",
  "description": "Skip, recréé de A à Z — messagerie, appels audio/vidéo, partage d'écran, groupes, sondages, Skip Credit, et tout le reste. Zéro dépendance.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "reset": "node server/seed.js --force",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["skip", "chat", "voip", "webrtc", "messaging", "video-call"],
  "license": "MIT",
  "private": true
}

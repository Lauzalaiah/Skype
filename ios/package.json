{
  "name": "skip-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Enveloppe iOS (Capacitor) de Skip. Isolée ici : le serveur et l'application web restent sans aucune dépendance.",
  "scripts": {
    "prepare:ios": "cap add ios && cap sync ios",
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  }
}
